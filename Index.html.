<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Protótipo Caça ao Tesouro</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f5f5f5;
      margin: 0;
      padding: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
    }
    #container {
      background: white;
      padding: 20px;
      max-width: 400px;
      width: 90%;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      border-radius: 8px;
      text-align: center;
    }
    button {
      background: #007bff;
      color: white;
      border: none;
      padding: 10px 20px;
      margin-top: 15px;
      border-radius: 5px;
      cursor: pointer;
      font-size: 1em;
    }
    button:disabled {
      background: #aaa;
      cursor: default;
    }
    img {
      margin-top: 15px;
      max-width: 100%;
      border-radius: 8px;
    }
  </style>
</head>
<body>
  <div id="container">
    <h2>Caça ao Tesouro - Protótipo</h2>
    <p id="text">Clique no botão para ver a primeira pista!</p>
    <button id="nextBtn">Próxima pista</button>
    <img id="finalImage" src="" alt="" style="display:none;" />
  </div>

  <script>
    const pistas = [
      "Pista 1: Procure onde guardamos as chaves.",
      "Pista 2: Agora, veja atrás do quadro na sala.",
      "Pista 3: Finalmente, verifique dentro do livro favorito.",
    ];

    const finalMsg = "Parabéns! Você encontrou o presente escondido! 🎉";

    const finalImgUrl = "https://upload.wikimedia.org/wikipedia/commons/thumb/4/42/Emoji_u1f389.svg/1024px-Emoji_u1f389.svg.png"; // emoji festa

    let etapa = -1;

    const textEl = document.getElementById("text");
    const btn = document.getElementById("nextBtn");
    const img = document.getElementById("finalImage");

    btn.addEventListener("click", () => {
      etapa++;
      if (etapa < pistas.length) {
        textEl.textContent = pistas[etapa];
      } else if (etapa === pistas.length) {
        textEl.textContent = finalMsg;
        img.src = finalImgUrl;
        img.style.display = "block";
        btn.disabled = true;
        btn.textContent = "Fim";
      }
    });
  </script>
</body>
</html>
